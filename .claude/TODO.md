# TODO — Отладка и улучшение idle-игры «Код и Кофе»

> Идём по порядку. Каждый пункт закрывается коммитом.

---

## [x] 1. Активность сессии и автосохранение в БД

- Автосохранение каждые 30 сек (сейчас только по событиям)
- Обновление `last_seen` в таблице `users` при каждом сохранении
- Проверить что сохранение не теряется при закрытии вкладки (`beforeunload`)
- Убедиться что гость не пишет в БД, а только в `localStorage`

---

## [x] 2. Фикс сброса прокачки prestigeShop после перезагрузки

- Воспроизвести баг: купить улучшение в магазине престижа → закрыть → открыть → улучшение сброшено
- Найти где `prestigeShop` теряется при загрузке (merge сохранений, `defaultState` перезаписывает)
- Убедиться что все поля `prestigeShop`, `prestige`, `prestigePoints` корректно мержатся при `loadSave`
- Проверить что `buy_prestige` в `ajax/save.php` сохраняет и возвращает корректный state

---

## [x] 3. Синхронизация — все данные аккаунта в БД

- Добавить в таблицу `users` или отдельную таблицу поля: `account_level`, `total_dungeon_clears`, `total_prestige_count`
- При сохранении (`action=save`) обновлять эти поля серверно из `save_data`
- При загрузке отдавать эти данные клиенту вместе с `save_data`
- Исключить дублирование: `leaderboard` и `game_saves` — источник истины, не `localStorage`

---

## [x] 4. Страница профиля — показывать все достижения

- Сравнить список достижений в `idle.js` (`ACHIEVEMENTS`) с тем что рендерится в `profile.php`
- Добавить вывод всех достижений из `save_data` с иконкой и описанием
- Заблокированные достижения показывать серыми с подсказкой условия
- Не открытые достижения скрывать описание (показывать `???`)

---

## [ ] 5. Таблица лидеров — исправить отображение «последний онлайн»

- Проверить как `updated_at` пишется в `leaderboard` (`strftime('%s','now')` = unix timestamp)
- В `leaderboard.php` исправить форматирование: перевести timestamp в читаемую дату
- Добавить relative time: «5 минут назад», «вчера», «3 дня назад»
- Убедиться что `last_seen` обновляется при каждом сохранении игры

---

## [ ] 6. Убрать экспорт/импорт/удаление аккаунта из UI

- `idle.php`: убрать кнопки `exportBtn`, `importBtn`, `resetBtn` из topbar
- Убрать соответствующие обработчики и функции из `idle.js`
- `profile.php`: убрать кнопку удаления аккаунта если есть
- CSS: убрать стили `.tb-actions` если они остаются пустыми

---

## [ ] 7. Адаптация под мобильный / TG Mini App

- Все страницы: viewport, touch events, font-size не меньше 14px
- `idle.php`: переработать layout под вертикальный экран (single column, tabs снизу или свайп)
- Кнопки: min 44px touch target
- Убрать hover-эффекты там где они мешают на touch
- `dungeon.php`: добавить виртуальный D-pad для мобильного управления
- `minigame.php`: проверить touch-события
- Подключить Telegram Web App SDK для будущей интеграции: `window.Telegram.WebApp`
- Проверить что нет горизонтального скролла ни на одной странице

---

## [ ] 8. Награды из активностей не зачисляются на аккаунт

- Воспроизвести баг: пройти подземелье / мини-игру → показывает что ЛОКи и OO зачислены → открыть idle → ничего не добавилось
- Проверить `ajax/save.php`: `dungeon_clear` и `minigame_reward` пишут в `game_saves` напрямую, но `idle.js` при загрузке может перезаписать это своим локальным `state`
- Найти race condition: `idle.js` делает save сразу после загрузки и перетирает то что записала активность
- Исправить: после возврата в `idle.php` — делать `loadSave` с сервера **перед** любым `save`, чтобы подтянуть награды
- Проверить `localStorage` vs серверный save — убедиться что не идёт merge в пользу старого локального state
- Затронутые файлы: `js/idle.js` (логика load/save при старте), `ajax/save.php` (`dungeon_clear`, `minigame_reward`)

---

## Прогресс

| # | Задача | Статус |
|---|---|---|
| 1 | Активность сессии и автосохранение | ✅ |
| 2 | Фикс сброса prestigeShop | ✅ |
| 3 | Синхронизация данных аккаунта | ✅ |
| 4 | Профиль — все достижения | ✅ |
| 5 | Лидерборд — последний онлайн | ⏳ |
| 6 | Убрать экспорт/импорт/удаление | ⏳ |
| 7 | Мобильная адаптация / TG Mini App | ⏳ |
| 8 | Награды из активностей не зачисляются | ⏳ |
